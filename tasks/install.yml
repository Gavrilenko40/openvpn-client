---
     - name: Install OpenVpn
       apt:
         pkg: openvpn_2.4*
         state: installed
         update_cache: yes
       notify:
           - openvpn@config start
       tags: 
         - openvpn install
       
