---
     - name: Install OpenVpn
       apt:
         pkg: openvpn_2.4.5-xenial0_amd64.deb
         state: installed
         update_cache: yes
       notify:
           - openvpn@config start
       tags: 
         - openvpn install
       
